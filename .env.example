# Copy to .env for local Docker Compose runs:  cp .env.example .env
# .env is git-ignored — never commit real credentials.

MONGO_USERNAME=ecomadmin
MONGO_PASSWORD=ecompassword

# Used to tag locally built images. Set to your Docker Hub account when pushing.
DOCKERHUB_USERNAME=local
IMAGE_TAG=latest

LOG_LEVEL=INFO
